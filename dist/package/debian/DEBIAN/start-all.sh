#/bin/bash


sudo start webida-auth
sudo start webida-build-jm
sudo start webida-build
sudo start webida-ntf
sudo start webida-conn
sudo start webida-cors-proxy
sudo start webida-debug
sudo start webida-fs
sudo start webida
sudo start webida-proxy


